{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<style> 
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; } 
</style>

            <nav class="category container d-flex">
                <div>
                    <a href="">souscat</a>
                </div>

                <div>
                    <a href="">souscat</a>
                </div>

                <div>
                    <a href="">soucat</a>
                </div>
            </nav>

			<div class="blockProduits container row">
				{% for produit in products %}
                <div class="card card-group col-3">
					<div class="card me-2" style="width: 15rem;">
						<img src="{{produit[0].image}}" class="card-img-top" alt="">
							<div class="card-body">
								<h5 class="card-title">{{produit[0].nom}}</h5>
								<p class="card-text">{{produit[0].description}}</p>
								<button type="button">Ajouter<img src="{{ asset('/img/cart.png') }}" alt=""></button>
								</div>
							</div>

					</div>
					{% endfor %}
			
                </div>
            </div>


{% endblock %}
