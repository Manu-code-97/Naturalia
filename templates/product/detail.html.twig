{% extends 'base.html.twig' %}

{# {% block title %}{{productDetail.nom}}{% endblock %} #}

{% block body %}



        <div id="produit" class="d-flex justify-content-evenly">
            <div id="image">
                <img src="{{asset('img/produit/' ~ productDetail.image)}}" alt="">
                <div id="favourite">
                <img src="/img/favourite.png" alt="">
                </div>
            </div>
            <div>
                <div id="description">
                    <h3>{{productDetail.nom}}</h3>
                    <p>{{productDetail.description}}</p>
                </div>
                <div id="label">
                    {% for label in productDetail.label %}
                        <img src="{{ asset('img/' ~ label.image) }}" alt="{{label.nom}}">
                    {% endfor %}
                </div>
                <div id="ajouter">
                    <button class="add-to-cart" data-product-id="{{ productDetail.id }}">Ajouter au panier<img src="/img/cart.png" alt=""></a></button>
                </div>
            </div>
        </div>
    <div class="d-flex justify-content-center" id="btn">
        <button>Origine</button>
        <button>Marque</button>
        <button>Ingrédient</button>
        <button>Producteur</button>
    </div>
    <div>
        <p class="d-flex justify-content-center">Lorem ipsum dolor sit amet, consectetur adipisicing elit.<br>Quam fugiat adipisci doloribus suscipit pariatur, modi facilis delectus molestiae.</p>
    </div>
   <div id="selections">
        <div class="container">
            <h3>Découvrez nos sélections</h3>
        </div>
        <div class="carousel container yellow">
       {{ include('/partials/carousel.html.twig', {'products':productsSelection}) }}
        </div>
   </div>

{% endblock %}
